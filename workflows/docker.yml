name: Docker Build CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Build Docker image
      run: docker build -t devops-app ./docker
    - name: Test container
      run: docker run -p 8080:80 devops-app curl localhost
